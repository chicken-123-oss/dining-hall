# 2026-06-23 工作日志

## 服务器部署与数据库升级（阿里云 ECS Linux）
- 服务器为阿里云 ECS（root@iZbp1bydso5n73ijvqbttnZ），Linux 系统，代码在 ~/dining-hall/
- `fuser -k 5001/tcp` 释放端口后 `python3 app.py &` 启动
- 手工执行 ALTER TABLE 添加 can_post/can_reply 列
- nginx 占用 5001 端口做反向代理，Flask 被挡，kill nginx 进程后 Flask 成功绑定

## 功能限制申诉系统（本次会话核心任务）

### 需求
- 用户被限制发帖后需申诉通道
- 用户能看到管理员审核结果

### 后端（app.py）
- 迁移：appeals 表新增 appeal_type TEXT 列（'ban' / 'restrict'）
- `/api/appeal` POST：扩展支持功能限制用户申诉，自动判断 appeal_type
- `process_appeal`：批准 restrict 类型申诉时恢复 can_post=1, can_reply=1
- 新增 `/api/my_appeals` GET：当前用户查看自己的申诉记录
- 403 错误提示更新：加入 `/appeal` 申诉链接
- 修复 sqlite3.Row 不支持 .get() 的 bug

### 前端
- **admin.html**：申诉列表新增「类型」列（封禁/功能限制）+ 类型筛选下拉 + 详情弹窗显示类型
- **appeal.html**：更新文案覆盖封禁和功能限制；新增申诉结果展示区（状态、类型、管理员回复）
- **index.html**：首页新增限制提醒横幅，显示「已限制发帖/回复 → 提交申诉 / 查看结果」
- **login.html**：无需改动（受限用户可正常登录，首页横幅负责提醒）

### 测试
- 端到端测试通过：创建受限用户 → 提交 restrict 申诉 → 管理员审批 → can_post 恢复为 1
- 本地服务器使用 managed Python：C:\Users\Think\.workbuddy\binaries\python\versions\3.13.12\python.exe
    30:
    31:## Python 环境变更
    32:- 用户重新安装了 Python，当前版本：Python 3.11.4（系统安装，路径 D:\Python\python\python.exe）
    33:- start.bat 中引用路径无需改动，仍为 D:\Python\python\python.exe
    34:- MEMORY.md 长期记忆已同步更新
